{
  "type": "object",
  "required": ["action", "version", "public_key", "order_id"],
  "properties": {
    "action": { "type": "string", "enum": ["refund", "hold_completion", "status"]},
    "version": { "type": "integer" },
    "public_key": { "type": "string" },
    "order_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "amount": { "type": "number" }
  }
}

